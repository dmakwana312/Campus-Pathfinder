import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({

    page: {
        position: "absolute",
        top: 0,
        right: 0,
        bottom: 0,
        left: 0,
        background: '#e3e3e3'
    }

}))

export { useStyles }